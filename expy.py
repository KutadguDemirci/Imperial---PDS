import numpy as np

linspace = np.linspace(0, 100, 10)   # 10 evenly spaced values between 0 and 100
arange = np.arange(0, 100, 10)       # values from 0 to <100, step size 10

print(f"linspace: {linspace}")
print(f"arange: {arange}")
