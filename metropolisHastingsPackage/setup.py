from setuptools import setup, find_packages

setup(
    name="MCMCSampling",
    version="0.0.1",
    packages=["mcmcsampling"],
    install_requires=["numpy>=1.0"],
    author="KutadguDemirci",
    description="Metropolis-Hastings MCMC sampling",
)